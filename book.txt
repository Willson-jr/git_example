Contents list:

1. GIT basic commands
        - git init
        - git status
        - git add
        - git rm --cached
        - git commit
        - git commit --amend
        - git log
	- git checkout --book.txt
	- git diff 
	- git reset HEAD book.txt
	- git stash
	- git checkout -b branch_name
	- git stash pop --index 0 //[]	//достать кусок с нужным  индексом
	- git checkout branch_name //создание
	- git merge (branch_name || origin/branch_name) // Обьеденение
	- git branch -d branch_name // удаление
	- git branch //список
	- git remote // spisok remotov
	- git remote -v //c adresami
	- git remote add (origin||dupa) remote_adress_url
	- git remote rm (origin || dupa) // удалит адрес 
